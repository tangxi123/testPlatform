<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.testplatform.mapper.UserMapper">
    <insert id="insertUser">
        insert into users(
           id, username, name, password, email, created_at, updated_at)
        values(
           #{id}, #{username}, #{name}, #{password}, #{email}, #{createdAt}, #{updatedAt}
        )
    </insert>
</mapper>